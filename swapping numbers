import java.util.Scanner;
class swap{
        public static void main(String[] args)
        {
            Scanner sc=new Scanner(System.in);
            int x=sc.nextInt();
            int y=sc.nextInt();
            int temp=0;
            temp=x;
            x=y;
            y=temp;
            System.out.println("the swappwd values are  values are"+x+" and "+y);
        }
}
    
